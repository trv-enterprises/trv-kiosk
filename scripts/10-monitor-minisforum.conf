# Minisforum M1 (Intel i9-12950HX) display config
# Uses modesetting driver with auto-detected resolution.
# Run `xrandr` to check output name and available modes.

Section "Device"
    Identifier  "Intel"
    Driver      "modesetting"
EndSection
